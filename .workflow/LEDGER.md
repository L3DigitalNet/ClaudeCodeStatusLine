# Statusline Python conversion ledger

- [x] RQ-001. Add an executable `statuslinepy` with a Python shebang and no filename extension. — evidence: `stat` reports mode 0755; line 1 is `#!/usr/bin/python3`.
- [x] RQ-002. Replicate the observable behavior and input/output contract of `statusline.sh` without tuning or enhancements. — evidence: 25 differential Bats cases pass; the fresh functional verifier confirmed status and byte parity for the adversarial valid-input cases.
- [x] RQ-003. Use both Rich and Humanize as runtime libraries in the conversion. — evidence: `statuslinepy` renders styles with Rich `Style.render` and formats token prefixes with `humanize.metric`; installed versions match `requirements.txt`.
- [x] RQ-004. Preserve repository conventions and existing user-owned changes, including `docs/TODO.md` and the local commit ahead of `origin/main`. — evidence: the pre-existing TODO diff was committed unchanged; `c154038` remains the direct parent of the conversion commit; reference files remain unchanged.
- [x] RQ-005. Add deterministic automated coverage that compares or otherwise pins the Python conversion to the Bash reference, following RED-GREEN-REFACTOR. — evidence: an isolated copy without `statuslinepy` failed the first differential test with exit 127; the integrated 25-case suite passes.
- [x] RQ-006. Pass the repository-defined relevant verification checks and a fresh adversarial verification of the integrated diff. — evidence: Python compile, `bash -n`, ShellCheck warning gate, and all 161 Bats tests pass; fresh functional and security verifiers returned confirmed verdicts.
- [x] RQ-007. Review the final diff, executable mode, dependency declarations, and working-tree state before any later-authorized integration; do not push or publish. — evidence: the pre-commit review found mode 0755, exact Rich/Humanize requirements, no generated bytecode, and no reference-file change; RQ-008 records the later-authorized local commit.
- [x] RQ-008. Commit every visible repository change on the current local branch without pushing. — evidence: `git commit` completed on `main`; the final status and local/remote-tracking comparison were verified after the ledger-closing amend.
- [x] RQ-009. Make extreme cached API reset timestamps degrade like Bash instead of aborting the status line. — evidence: differential Bats test 150 and the final security inspection confirm guarded ISO conversion.
- [x] RQ-010. Make JSON-file and config-path encoding independent of the process locale while preserving Bash-compatible opaque-byte behavior. — evidence: differential Bats tests 151-152 pass in the 179-test gate.
- [x] RQ-011. Document and verify the `/usr/bin/python3` runtime dependency contract for Rich and Humanize. — evidence: INSTALL/README name Python 3.10+ and exact pins; `/usr/bin/python3` imports both installed pinned versions.
- [x] RQ-012. Record `statuslinepy` as the third behavioral mirror in tracked release/install documentation and the local repository convention. — evidence: inspected README, INSTALL, CHANGELOG, and ignored local CLAUDE.md.
- [x] RQ-013. Match jq `//` fallback semantics for valid JSON fields whose value is `false`. — evidence: differential Bats test 154 passes.
- [x] RQ-014. Match Bash command-substitution behavior for newline-only stdin. — evidence: raw differential Bats test 153 passes.
- [x] RQ-015. Preserve opaque Git output bytes and stdout from nonzero `git diff --numstat` commands. — evidence: raw differential Bats tests 155-156 pass.
- [x] RQ-016. Match Bash for scalar `seven_day`, first-only model suffix replacement, literal `null` credential fallthrough, and nonnumeric version components. — evidence: differential Bats tests 157-162 pass; final functional comparator matrix matched Bash.
- [x] RQ-017. Add focused differential regressions for every corrected runtime finding and capture the expected RED failures before implementation. — evidence: reconstructed HEAD candidate failed all 18 current correction regressions; integrated tests pass.
- [x] RQ-018. Pass the complete repository gate plus fresh functional and security verification of the integrated corrections. — evidence: artifact-free compile, Bash syntax, ShellCheck, and all 179 Bats tests pass; targeted functional/security follow-ups closed every reported finding.
- [x] RQ-019. Preserve `statusline.sh`, `statusline.ps1`, unrelated history, and the no-push state; leave corrections uncommitted unless separately authorized. — evidence: protected-path diff is empty; HEAD remains `15c3742`, main is ahead two, and no commit or push occurred.
- [x] RQ-020. Make escaped lone-surrogate JSON and embedded-NUL command arguments degrade without aborting, preserving Bash-observable output. — evidence: raw differential Bats tests 163-165 pass; final security verifier confirmed both crash paths closed.
- [x] RQ-021. Match jq failure semantics for `false` intermediate model/workspace objects, decisively test both opaque config hashes, and state implementation-specific `jq` requirements accurately. — evidence: tests 152 and 166-167 pass; inspected README/INSTALL requirements.
- [x] RQ-022. Add RED-first differential regressions for the second-stage verifier findings, then repeat fresh functional/security verification before closure. — evidence: reconstructed HEAD failed all 18 regressions; current 179-test gate and both targeted verifier follow-ups pass.
- [x] RQ-023. Match Bash `read` semantics when version strings contain more than three dot-separated components, with a RED-first comparator regression. — evidence: Bats test 161 and the final eight-case Bash/Python comparator matrix pass.
- [x] RQ-024. Document Python 3.10+ exactly, match Bash numeric-overflow comparison failures, and make the opaque-Git test independent of global commit signing. — evidence: test 162 passes; final security verifier confirmed docs, bounds, and `commit.gpgSign=false`.
